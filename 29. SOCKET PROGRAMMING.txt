	--- socket ----
client  --- bind  -----    server
	---listen -----    
	---send/req ---  
	---close------
# we are creating server here
import socket
host = 'localhost'
port = 4000
socket.socket(socket.AF_INET, socket.SOCK_STREAM)  # AF_INET SOCK_STREAM default values
s.bind((host, port)
s.listen(1) # number indicates no'of clinets
c, addr = s.accept()  # s.accept returns, client and its address
print("connection from: ", str(addr))
c.send(b"Hello, how are you")
c.send("bye", encode()) # encode or b converts into binary

# we are creating client
import socket
s = socket.socket()
s.connect(("localhost", 4000)
msg = s.recv(1024)
while msg:
	print("Received: ", msg.decode())
	msg = s.recv(1024)
c.close()